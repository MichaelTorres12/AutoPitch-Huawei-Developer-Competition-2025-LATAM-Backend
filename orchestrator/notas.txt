# Levantar e instlaar todo:
py -3.11 -m venv .venv

# Levantar el servidor:
.\.venv\Scripts\Activate.ps1

# Instalar dependencias python
pip install --upgrade pip / python.exe -m pip install --upgrade pip

# despues:
pip install -r requirements.txt --no-cache-dir

# Levantar el servidor:
.\.venv\Scripts\Activate.ps1

#Agregar lo siguiente siempre dentro de la terminal con el entorno encendido:
python -c "from src.main import app; print('✅ App imports successfully')"

# Levantar el servidor
python -m uvicorn src.main:app --reload --port 8080

# Comprobar que funciona:
http://127.0.0.1:8080/health
# Ping a la DB
http://127.0.0.1:8080/db/ping


#
pip install python-multipart

python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8080

## * Emiliano trabajar los nuevos endpoints para el Orchestrator (chatGPT revisar chat)